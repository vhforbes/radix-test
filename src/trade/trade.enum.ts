export enum TradeStatus {
  Awaiting = 'awaiting',
  Active = 'active',
  Closed = 'closed',
  Canceled = 'canceled',
}

export enum TradeMarkets {
  Futures = 'futures',
  Sport = 'spot',
}

export enum TradeResult {
  Gain = 'gain',
  Loss = 'loss',
  Even = 'even',
}

export enum TradeDirection {
  Short = 'short',
  Long = 'long',
}
