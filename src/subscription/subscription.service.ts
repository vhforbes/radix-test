import { Injectable } from '@nestjs/common';

// TODOs:
// * [] Create a new SubscriptionPlan
// * [] Create a subscription based on a plan (will call payment services)
// * [] Renew a subscription
// * [] Keep track of subscriptions and renewals
// * [] Cancel a subscription
// * [] Verify if a subscription is valid to access the resource

@Injectable()
export class SubscriptionService {}
